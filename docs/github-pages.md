# GitHub Pages deployment

The repository is deployable without a build step.

1. In the GitHub repository, open **Settings → Pages**.
2. Choose **Deploy from a branch**.
3. Select `main` and `/ (root)`.
4. Save and wait for the deployment status to become successful.

The site is expected at `https://scooterxd69.github.io/Science-Project/` once Pages is enabled.
